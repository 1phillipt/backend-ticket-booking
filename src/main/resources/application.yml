spring:
  application:
    name: BookTicket
  datasource:
    url: jdbc:mysql://localhost:3306/ticket_booking
    username: root
    password: Root123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: true

server:
  port: 8081